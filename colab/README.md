# Stage 1 — Google Colab

Put your training notebook here (e.g. `train_finbert.ipynb`).
Use a GPU runtime, fine-tune FinBERT, evaluate, then export and download
`finbert-finetuned.zip` to unzip into `../backend/model/`.

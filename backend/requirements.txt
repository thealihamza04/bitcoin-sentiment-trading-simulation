# --- API (light, fast to install) ---
fastapi
uvicorn[standard]
pydantic

# --- Data / simulation ---
pandas
numpy
pyarrow
yfinance

# --- News dataset loading ---
datasets

# --- Inference (heavy — install when the model is ready) ---
# On a CPU-only machine, install torch from the smaller CPU wheel first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
transformers
